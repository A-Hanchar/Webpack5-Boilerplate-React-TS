import React from 'react'

import styles from './styles.css'

export const Footer = () => (
  <footer className={styles.footer}>
    <div className={styles.container}>Footer Content</div>
  </footer>
)
